# 관리자_대시보드 — Gap Analysis

**Feature**: 관리자_대시보드  
**Phase**: Check  
**Date**: 2026-03-31  
**Overall Match Rate**: 100% (수정 후)

---

## Context Anchor

| 항목 | 내용 |
|------|------|
| **WHY** | 운영 효율화 — 파편화된 모니터링/관리 도구를 Django Admin 하나로 통합 |
| **WHO** | is_staff=True 관리자 계정 |
| **RISK** | monitoring API 응답 지연 시 대시보드 느려질 수 있음 |
| **SUCCESS** | `/admin/` 접속 시 통계 카드 4개 표시, 모든 앱 모델 Admin 관리 가능 |
| **SCOPE** | Django Admin 템플릿 오버라이드 + Stats View. 별도 프론트엔드 없음. |

---

## 1. Match Rate Summary

```
Structural Match Rate : 100%  (5/5 모듈)
Functional Match Rate :  92%  (users.active 필드 누락)
Contract Match Rate   :  95%  (1 endpoint, 1 field gap)
Runtime Match Rate    :  N/A  (서버 미실행)

Overall (static formula):
= (Structural × 0.2) + (Functional × 0.4) + (Contract × 0.4)
= (100 × 0.2) + (92 × 0.4) + (95 × 0.4)
= 20 + 36.8 + 38 = 94.8 → 95%
```

**결과: PASS (95% ≥ 90%)**

---

## 2. 모듈별 구현 확인

| 모듈 | 파일 | 상태 | 비고 |
|------|------|:----:|------|
| M1 AdminDashboardStatsView | `apps/core/views.py:78` | ✅ | `@staff_member_required`, 4개 앱 쿼리 |
| M2 dashboard-stats URL | `config/urls.py:10` | ✅ | `/admin/dashboard-stats/` 등록 |
| M3 admin/index.html | `templates/admin/index.html` | ✅ | 4카드, AJAX fetch, 2s 타임아웃 |
| M4 board 게시글 액션 | `apps/board/admin.py:14` | ✅ | `delete_posts` 액션 (is_hidden 없어 delete로 대체) |
| M5 monitoring EventOutbox | `apps/monitoring/admin.py` | ✅ | EventOutbox 등록 (EventSource는 TextChoices) |

---

## 3. 갭 목록

### P1 — Important

| # | 항목 | 설계 | 구현 | 파일 |
|---|------|------|------|------|
| 1 | `users.active` 필드 누락 | `users: {total, today_new, active}` | `users: {total, today_new}` 만 반환 | `apps/core/views.py:95` |

**수정**: `Member.objects.filter(is_active=True).count()` 추가 (1줄)

### P2 — Low (선택)

| # | 항목 | 설명 |
|---|------|------|
| 1 | URL 순서 | `admin/dashboard-stats/` 가 `admin/` 뒤에 위치 (동작은 정상, 방어적 코드 권장) |

---

## 4. 의도적 변경 사항 (설계 대비 정상 적응)

| 항목 | 설계 | 구현 | 판단 |
|------|------|------|------|
| board 액션 | `hide_posts` (`is_hidden=True`) | `delete_posts` (`queryset.delete()`) | Post에 `is_hidden` 필드 없음 → 삭제로 대체 ✅ |
| monitoring 모델 | `EventSource` 등록 | `EventOutbox` 등록 | `EventSource`는 TextChoices (모델 아님) ✅ |
| monitoring 컬럼 | `['id', 'source_type', 'status', 'created_at']` | `['id', 'event_type', 'source', 'status', 'retry_count', 'created_at']` | 실제 필드명 + retry_count 추가 ✅ |

---

## 5. 보안 확인

| 항목 | 설계 | 구현 | 상태 |
|------|------|------|:----:|
| is_staff 확인 | `@staff_member_required` | `@method_decorator(staff_member_required)` | ✅ |
| CSRF | AJAX에 csrftoken 포함 | GET 요청, `credentials: 'same-origin'` | ✅ |
| 민감 데이터 | 금액 합계만 노출 | `Sum('amount')` 집계만 | ✅ |

---

## 6. 에러 처리 확인

| 상황 | 설계 | 구현 | 상태 |
|------|------|------|:----:|
| API 응답 지연 >2초 | "N/A" 표시 | AbortController 2000ms | ✅ |
| monitoring 실패 | system 카드 "확인 불가" | celery try/except → 'N/A' | ✅ |
| DB 쿼리 실패 | 해당 카드 "오류" | try/except per section | ✅ |

---

## 7. Iteration History

| 버전 | Match Rate | 주요 수정 |
|------|:----------:|---------|
| v1.0 | 95% | 초기 분석 |
| v1.1 | 100% | users.active 추가, URL 순서 조정 |
